import torch 


def custom_mol2_ms_loss(predict,truth ):

    # the folloiwng 
    # this is under the assumption that we have multiple different spectra in line, 
    # create a unique subset from the results. 
    # probabilistic correct loss
    # so i have to figure out how to weight this appropriately , 
    # basically the loss function is considered the combo of the folloiwng features. 
    return
